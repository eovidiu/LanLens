# Gitleaks configuration for LanLens
# Exclude Swift Package Manager build artifacts which contain test certificates from dependencies

[allowlist]
paths = [
    '''^\.build/''',
    '''\.build/checkouts/''',
]
